/**
 * Defines the protected routes for the application.
 */
export const protectedRoutes = ['/'];

/**
 * Public routes that should be accessible without authentication.
 */
export const publicRoutes = ['/auth/signin'];
